[Container]
Image=docker.io/library/postgres:16
Volume=pgdata.volume:/var/lib/postgresql/data

[Container]
; The same section repeated. systemd treats this as a continuation of the
; first occurrence rather than an error.
Environment=POSTGRES_PASSWORD=hunter2

[Install]
