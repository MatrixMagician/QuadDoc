# Generated by quaddoc from a compose project.
#
# VolumeName= makes Podman name the volume `cachedata`, rather than
# Quadlet's default of `systemd-cachedata`. Reference it from a container as
# `cachedata.volume`, not by the bare name.

[Volume]
VolumeName=cachedata
