# Generated by quaddoc from a compose project.
#
# VolumeName= makes Podman name the volume `pgdata`, rather than Quadlet's
# default of `systemd-pgdata`. Reference it from a container as
# `pgdata.volume`, not by the bare name.

[Volume]
VolumeName=pgdata
